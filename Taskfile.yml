version: "3"

vars:
  COMPOSE_FILE: docker-compose.local.yml

tasks:
<<<<<<< HEAD
  default:
    desc: Show available tasks
    cmds:
      - task --list

  setup:
    desc: First-time setup — .env, Docker, deps, migrations
    cmds:
      - task: env
      - task: up
      - npm install
      - npm run db:migrate:deploy
      - mkdir -p storage
      - echo ""
      - echo "Ready. Run  task dev  then open http://localhost:3000/register"

  env:
    desc: Create .env from .env.example if missing
    status:
      - test -f .env
    cmds:
      - cp .env.example .env
      - echo "Created .env from .env.example"

  up:
    desc: Start Postgres + Redis
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} up -d --wait
      - docker compose -f {{.COMPOSE_FILE}} ps

  down:
    desc: Stop Postgres + Redis
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} down

  dev:
    desc: Start Next.js dev server
    deps:
      - up
    cmds:
      - npm run dev

  logs:
    desc: Tail Docker service logs
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} logs -f --tail 50

  ps:
    desc: Show Docker service status
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} ps

  migrate:
    desc: Apply database migrations
    cmds:
      - npm run db:migrate:deploy

  reset-db:
    desc: Wipe database volume and re-run migrations (destructive)
    prompt: This deletes all local database data. Continue?
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} down -v
      - task: setup
=======
  setup:
    desc: First-time local setup (env, Docker, deps, migrations)
    cmds:
      - test -f .env || cp .env.example .env
      - docker compose -f {{.COMPOSE_FILE}} up -d
      - npm install
      - npm run db:migrate:deploy

  up:
    desc: Start database/redis and dev server
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} up -d
      - npm run dev

  down:
    desc: Stop database and redis containers
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} down

  db:up:
    desc: Start only PostgreSQL and Redis
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} up -d

  db:status:
    desc: Check if database is reachable
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} ps
      - curl -s http://localhost:3000/api/ping || true

  db:migrate:
    desc: Apply pending database migrations
    cmds:
      - npm run db:migrate:deploy
>>>>>>> 63fd26e (Add database health checks and clearer setup when Postgres is down)
